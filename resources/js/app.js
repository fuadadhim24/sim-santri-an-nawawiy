import './bootstrap';

// Livewire scripts are loaded via the @livewireScripts directive in Blade.
// If you prefer bundling the client with Vite, install the `@livewire/livewire`
// npm package and uncomment the import below.
// import 'livewire';

import Swal from 'sweetalert2';
window.Swal = Swal;
